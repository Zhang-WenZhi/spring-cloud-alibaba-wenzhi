<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenzhi.leetcode_service.dao.JobTaskDao">
    <select id="findAllEnabled" resultType="com.wenzhi.leetcode_service.entity.JobTask">
        SELECT * FROM job_task WHERE enabled = true
    </select>
    <delete id="deleteLogsBefore">
        <![CDATA[
            DELETE FROM job_task_log
            WHERE start_time < #{threshold}
        ]]>
    </delete>
    <select id="selectById" resultType="com.wenzhi.leetcode_service.entity.JobTask">
        SELECT * FROM job_task WHERE id = #{id}
    </select>

    <select id="selectEnabledTasks" resultType="com.wenzhi.leetcode_service.entity.JobTask">
        SELECT * FROM job_task WHERE enabled = true
    </select>

    <update id="updateWithLock2">
        UPDATE job_task
        SET task_name = #{taskName},
        task_class = #{taskClass},
        method_name = #{methodName},
        trigger_type = #{triggerType},
        cron_expression = #{cronExpression},
        interval_millis = #{intervalMillis},
        enabled = #{enabled},
        version = version + 1
        WHERE id = #{id} AND version = #{version}
    </update>

    <insert id="insertLog2">
        INSERT INTO job_task_log (task_id, status, start_time, end_time, error_info, duration, trigger_type, trigger_conf)
        VALUES (#{taskId}, #{status}, #{startTime}, #{endTime}, #{errorInfo}, #{duration}, #{triggerType}, #{triggerConf})
    </insert>
</mapper>